# Connection string de Neon (Dashboard → Connect → Pooled connection).
DATABASE_URL="postgresql://usuario:clave@ep-xxx.aws.neon.tech/neondb?sslmode=require"

# Firma las cookies de sesión. Generala con: openssl rand -base64 32
AUTH_SECRET="cambiame"
